# 房源组合最优解计算器 — 软件开发规划

## 一、项目概述

| 项目 | 说明 |
|------|------|
| 应用名称 | 房源组合最优解计算器 |
| 技术栈 | Tauri 2.0 + Rust + React 18 + TypeScript |
| 目标平台 | macOS / Windows / Linux 桌面本地运行 |
| 核心功能 | 输入房源户型与数量，定义组合规则，自动计算最优解并可视化 |
| 离线运行 | 全部计算在本地完成，不依赖网络服务 |

---

## 二、需求分析

### 2.1 业务场景

房产开发商或销售团队拥有多种户型房源（如 50㎡、70㎡、90㎡、120㎡、150㎡），需要将它们打包成不同的"组合方案"进行销售。例如：

- **组合 A**：1 套 50㎡ + 2 套 70㎡ + 1 套 90㎡（共 4 套）
- **组合 B**：1 套 120㎡ + 1 套 150㎡（共 2 套）
- **组合 C**：1 套 70㎡ + 2 套 90㎡（共 3 套）

用户指定某个组合的销售数量后，系统自动计算其余组合的最优数量，使剩余房源尽可能少。

### 2.2 功能需求

| 编号 | 功能 | 描述 |
|------|------|------|
| F1 | 房源类型管理 | 添加/编辑/删除户型类型（名称、面积、数量） |
| F2 | 组合规则管理 | 添加/编辑/删除组合方案，定义每个组合包含哪些户型及数量 |
| F3 | 手动输入组合数量 | 用户指定任意一个或多个组合的销售数量 |
| F4 | 最优解计算 | 根据剩余房源自动计算其他组合的最优数量 |
| F5 | 网格可视化 | 中心区域显示与总房源数对应的小方格，按组合着色 |
| F6 | 剩余房源展示 | 显示各户型剩余数量和总剩余数量 |
| F7 | 数据持久化 | 本地保存房源和组合配置，下次打开自动恢复 |
| F8 | 方案导出 | 导出计算结果为图片或 JSON |

### 2.3 非功能需求

- **响应速度**：1000 套以内房源，最优解计算 < 100ms
- **界面流畅**：网格渲染 60fps，着色动画流畅
- **跨平台一致**：macOS / Windows 界面风格统一
- **安装包体积**：< 15MB（Tauri 优势）

---

## 三、技术选型

### 3.1 整体架构

```
┌──────────────────────────────────────────────┐
│              Tauri 应用容器                    │
│  ┌──────────────────────────────────────┐    │
│  │          前端 (WebView)              │    │
│  │   React 18 + TypeScript + Vite       │    │
│  │   状态管理: Zustand                   │    │
│  │   可视化: Canvas / SVG                │    │
│  │   UI 组件: Tailwind CSS               │    │
│  └──────────────┬───────────────────────┘    │
│           Tauri IPC (invoke)                  │
│  ┌──────────────┴───────────────────────┐    │
│  │          后端 (Rust)                  │    │
│  │   最优解求解器 (ILP / 贪心+回溯)       │    │
│  │   数据持久化 (SQLite / JSON 文件)      │    │
│  │   Tauri Commands (IPC 接口层)         │    │
│  └──────────────────────────────────────┘    │
└──────────────────────────────────────────────┘
```

### 3.2 技术清单

| 层级 | 技术 | 用途 | 选择理由 |
|------|------|------|----------|
| 框架 | Tauri 2.0 | 桌面应用容器 | 包体小、Rust 后端性能强、安全沙箱 |
| 前端框架 | React 18 | UI 构建 | 生态成熟、组件化开发 |
| 语言 | TypeScript | 前端类型安全 | 类型推导、减少运行时错误 |
| 构建 | Vite 5 | 前端打包 | HMR 快、Tauri 原生集成 |
| 样式 | Tailwind CSS 3 | 原子化 CSS | 开发速度快、包体可控 |
| 状态 | Zustand | 全局状态管理 | 轻量、TypeScript 友好 |
| 可视化 | HTML Canvas | 网格渲染 | 大量方格时性能优于 SVG |
| 后端语言 | Rust | 核心算法 | 性能、安全、Tauri 原生 |
| 求解器 | good_lp crate | 整数线性规划 | Rust 原生、支持多种 solver 后端 |
| 持久化 | serde_json + 文件 | 配置存储 | 轻量、无需数据库依赖 |

### 3.3 为什么用 Tauri 而非 Electron

| 对比项 | Tauri | Electron |
|--------|-------|----------|
| 安装包体积 | ~5-15MB | ~80-150MB |
| 内存占用 | 低（系统 WebView） | 高（内嵌 Chromium） |
| 后端语言 | Rust（高性能） | Node.js |
| 安全性 | 白名单 IPC、能力沙箱 | 相对宽松 |
| 适用场景 | 本地计算密集型应用 | 通用桌面应用 |

本项目核心算法用 Rust 实现可以直接获得原生性能，且包体小、安装快，适合作为内部工具分发。

---

## 四、核心算法设计

### 4.1 问题数学建模

**已知：**
- 户型类型：`T = [t1, t2, ..., tn]`，对应库存数量 `Q = [Q1, Q2, ..., Qn]`
- 组合方案：`C1, C2, ..., Cm`，其中组合 `Ck` 对户型 `tj` 的需求为 `c[k][j]`
- 用户指定的组合数量：`x[1] = 用户输入值`（可指定多个）

**求解：** 其余组合的数量 `x[2], x[3], ..., x[m]`

**目标函数：**

```
最小化：剩余房源总数 = Σ Qj - Σ (xk × Σ c[k][j])
```

等价于：

```
最大化：已使用房源总数 = Σ (xk × Σ c[k][j])
```

**约束条件：**

```
∀j: Σ (xk × c[k][j]) ≤ Qj    （每种户型使用量不超过库存）
∀k: xk ≥ 0 且为整数             （组合数量非负整数）
```

### 4.2 算法选择

这是一个 **整数线性规划（ILP）问题**。根据问题规模选择不同策略：

| 场景 | 策略 | 说明 |
|------|------|------|
| 通用场景 | ILP 求解器 | 使用 `good_lp` crate 调用 `cbc` 或 `lp-solver` 后端 |
| 小规模（户型≤5，组合≤10） | 回溯+剪枝 | 自实现回溯搜索，精确解，无外部依赖 |
| 降级方案 | 贪心算法 | 当 ILP 不可用时，贪心+局部优化兜底 |

**推荐方案：ILP 优先 + 贪心降级**

```
输入: 户型库存 Q, 组合矩阵 C, 用户指定组合数量 fixed_x
输出: 各组合数量 x, 各户型剩余 r

1. 扣除用户指定组合占用的房源: R[j] = Q[j] - Σ(fixed_x[k] × C[k][j])
2. 检查 R[j] ≥ 0（用户输入是否合法）
3. 构造 ILP: max Σ(xk × ΣC[k][j]) s.t. Σ(xk × C[k][j]) ≤ R[j], xk ∈ Z≥0
4. 调用 good_lp 求解
5. 若求解失败（超时/无解），降级为贪心算法
6. 计算剩余: r[j] = R[j] - Σ(xk × C[k][j])
7. 返回结果
```

### 4.3 贪心降级算法

当 ILP 求解器不可用时，使用贪心策略：

```
1. 计算每个组合的"效率" = 组合占用总套数 / 组合中最大户型需求
2. 按效率降序排列组合
3. 对每个组合，尝试尽可能多地分配数量，直到某户型库存不足
4. 回溯尝试其他组合
5. 取所有尝试中的最优解
```

### 4.4 多最优解处理

当存在多个使用相同总数量的方案时，按以下优先级选择：
1. 剩余户型种类最少（尽量集中剩余在某几种户型）
2. 组合数量方差最小（分布更均匀）
3. 组合编号最小的方案优先

---

## 五、UI/UX 设计

### 5.1 界面布局

```
┌────────────────────────────────────────────────────────┐
│  房源组合最优解计算器                    [导出] [设置]   │
├──────────────┬───────────────────────┬────────────────┤
│              │                       │                │
│  输入面板     │    网格可视化区域       │   结果面板      │
│  (左侧 280px) │    (中间 自适应)       │  (右侧 280px)   │
│              │                       │                │
│ ┌──────────┐ │  ┌─┐┌─┐┌─┐┌─┐┌─┐    │ 组合A: 5 个     │
│ │ 房源类型   │ │  │A││A││A││A││A│    │ 组合B: 8 个     │
│ │ 50㎡ × 20│ │  └─┘└─┘└─┘└─┘└─┘    │ 组合C: 6 个     │
│ │ 70㎡ × 30│ │  ┌─┐┌─┐┌─┐┌─┐┌─┐    │                │
│ │ 90㎡ × 25│ │  │B││B││B││B││B│    │ 已用: 90 套     │
│ │ 120㎡× 15│ │  └─┘└─┘└─┘└─┘└─┘    │ 剩余: 10 套     │
│ │ 150㎡× 10│ │  ┌─┐┌─┐┌─┐┌─┐┌─┐    │                │
│ │ [+ 添加]  │ │  │C││C││C││C││C│    │ 剩余明细:       │
│ └──────────┘ │  └─┘└─┘└─┘└─┘└─┘    │ 50㎡: 0 套     │
│              │  ┌─┐┌─┐┌─┐┌─┐┌─┐    │ 70㎡: 3 套     │
│ ┌──────────┐ │  │C││C││░││░││░│    │ 90㎡: 2 套     │
│ │ 组合定义   │ │  └─┘└─┘└─┘└─┘└─┘    │ 120㎡: 5 套    │
│ │ A: 1+2+1  │ │  (灰色=未使用)        │ 150㎡: 0 套    │
│ │ B: 1+1    │ │                       │                │
│ │ C: 1+2    │ │  共 100 套            │ [重新计算]      │
│ │ [+ 添加]  │ │                       │                │
│ └──────────┘ │                       │                │
│              │                       │                │
│ ┌──────────┐ │                       │                │
│ │ 手动输入   │ │                       │                │
│ │ 组合A=[ 5]│ │                       │                │
│ │ [计算]    │ │                       │                │
│ └──────────┘ │                       │                │
├──────────────┴───────────────────────┴────────────────┤
│ 状态栏: 已加载配置 | 上次计算耗时 23ms                   │
└────────────────────────────────────────────────────────┘
```

### 5.2 网格可视化规格

| 参数 | 规则 |
|------|------|
| 方格总数 | = 户型总套数（如 100 套 → 100 格） |
| 排列方式 | 尽量正方形排列：rows = ceil(sqrt(n)), cols = ceil(n/rows) |
| 方格大小 | 自适应容器尺寸：size = min(containerW/cols, containerH/rows) |
| 颜色分配 | 每种组合分配独立色相，色相均匀分布在色环上 |
| 未使用方格 | 灰色填充（#E0E0E0），表示剩余房源 |
| 着色顺序 | 按组合编号顺序依次填充方格 |
| 动画 | 着色时逐格亮起（CSS transition），从左到右、从上到下 |
| 悬浮提示 | 鼠标悬停在方格上显示：户型类型 + 所属组合 |

### 5.3 颜色方案

采用 9 种高对比度颜色循环分配：

| 组合编号 | 颜色 | 色值 | 示例 |
|----------|------|------|------|
| 组合 A | 蓝色 | #378ADD | ██████ |
| 组合 B | 绿色 | #639922 | ██████ |
| 组合 C | 珊瑚色 | #D85A30 | ██████ |
| 组合 D | 紫色 | #7F77DD | ██████ |
| 组合 E | 琥珀色 | #EF9F27 | ██████ |
| 组合 F | 青色 | #1D9E75 | ██████ |
| 组合 G | 粉色 | #D4537E | ██████ |
| 组合 H | 红色 | #E24B4A | ██████ |
| 组合 I | 灰色 | #888780 | ██████ |
| 未使用 | 浅灰 | #E0E0E0 | ██████ |

超过 9 种组合时，使用 HSL 色环自动生成。

### 5.4 交互流程

```
用户操作流程:

1. 添加户型类型
   └─ 输入名称 + 面积 + 数量 → 自动更新总套数 → 网格更新

2. 定义组合方案
   └─ 选择户型 + 设置每种户型需求数量 → 保存组合

3. 输入手动销售数量
   └─ 选择组合 → 输入数量 → 点击"计算"

4. 系统计算最优解
   └─ 调用 Rust 后端求解 → 返回结果 → 更新网格着色 → 显示剩余明细

5. 调整手动数量
   └─ 修改输入 → 重新计算 → 网格重新着色
```

---

## 六、项目目录结构

```
房源组合最优解/
├── src-tauri/                    # Rust 后端
│   ├── Cargo.toml                # Rust 依赖配置
│   ├── tauri.conf.json           # Tauri 应用配置（窗口、权限等）
│   ├── build.rs                  # 构建脚本
│   ├── icons/                    # 应用图标
│   ├── capabilities/             # Tauri 权限配置
│   └── src/
│       ├── main.rs               # 程序入口
│       ├── lib.rs                # 库入口，注册 commands
│       ├── commands.rs           # Tauri IPC 命令定义
│       ├── solver/
│       │   ├── mod.rs            # 求解器模块入口
│       │   ├── ilp.rs            # ILP 求解器（good_lp）
│       │   ├── greedy.rs         # 贪心降级算法
│       │   └── models.rs         # 求解器数据模型
│       └── storage/
│           ├── mod.rs            # 存储模块入口
│           └── json_store.rs     # JSON 文件读写
│
├── src/                          # React 前端
│   ├── main.tsx                 # React 入口
│   ├── App.tsx                  # 根组件
│   ├── components/
│   │   ├── InputPanel/
│   │   │   ├── index.tsx         # 输入面板容器
│   │   │   ├── HouseTypeEditor.tsx   # 户型编辑器
│   │   │   ├── CombinationEditor.tsx # 组合编辑器
│   │   │   └── ManualInput.tsx   # 手动数量输入
│   │   ├── GridCanvas/
│   │   │   ├── index.tsx         # 网格画布容器
│   │   │   ├── GridRenderer.tsx  # Canvas 渲染逻辑
│   │   │   └── ColorMapper.tsx   # 颜色映射
│   │   ├── ResultPanel/
│   │   │   ├── index.tsx         # 结果面板容器
│   │   │   ├── CombinationResult.tsx # 组合结果
│   │   │   └── RemainingDetail.tsx   # 剩余明细
│   │   └── common/
│   │       ├── Header.tsx        # 顶部栏
│   │       └── StatusBar.tsx     # 底部状态栏
│   ├── hooks/
│   │   ├── useSolver.ts          # 调用求解器 hook
│   │   └── usePersistentConfig.ts # 配置持久化 hook
│   ├── store/
│   │   └── useAppStore.ts        # Zustand 全局状态
│   ├── types/
│   │   └── index.ts             # TypeScript 类型定义
│   ├── utils/
│   │   ├── grid.ts              # 网格布局计算
│   │   └── colors.ts            # 颜色生成工具
│   └── styles/
│       └── global.css           # 全局样式
│
├── package.json
├── tsconfig.json
├── vite.config.ts
├── tailwind.config.js
├── index.html
└── README.md
```

---

## 七、数据模型定义

### 7.1 核心数据结构（TypeScript）

```typescript
// 户型类型
interface HouseType {
  id: string;
  name: string;       // 如 "50㎡"
  area: number;       // 面积（可选，用于显示）
  quantity: number;   // 库存数量
}

// 组合方案
interface Combination {
  id: string;
  name: string;       // 如 "组合A"
  color: string;      // 颜色色值
  items: CombinationItem[]; // 组合包含的户型及数量
}

interface CombinationItem {
  typeId: string;     // 对应 HouseType.id
  count: number;      // 该户型在此组合中的数量
}

// 手动输入
interface ManualInput {
  combinationId: string;
  quantity: number;
}

// 求解结果
interface SolveResult {
  assignments: CombinationAssignment[]; // 各组合分配数量
  remaining: RemainingItem[];            // 各户型剩余
  totalUsed: number;                     // 已使用总数
  totalRemaining: number;                // 剩余总数
  solveTime: number;                     // 求解耗时(ms)
  algorithm: 'ilp' | 'greedy';          // 使用的算法
}

interface CombinationAssignment {
  combinationId: string;
  quantity: number;    // 分配的数量
  isManual: boolean;   // 是否为手动输入
}

interface RemainingItem {
  typeId: string;
  typeName: string;
  remaining: number;
}
```

### 7.2 Rust 端数据结构

```rust
use serde::{Deserialize, Serialize};

#[derive(Serialize, Deserialize, Clone)]
pub struct HouseType {
    pub id: String,
    pub name: String,
    pub quantity: u32,
}

#[derive(Serialize, Deserialize, Clone)]
pub struct Combination {
    pub id: String,
    pub name: String,
    pub items: Vec<CombinationItem>,
}

#[derive(Serialize, Deserialize, Clone)]
pub struct CombinationItem {
    pub type_id: String,
    pub count: u32,
}

#[derive(Serialize, Deserialize)]
pub struct SolveRequest {
    pub house_types: Vec<HouseType>,
    pub combinations: Vec<Combination>,
    pub manual_inputs: Vec<(String, u32)>, // (combination_id, quantity)
}

#[derive(Serialize, Deserialize)]
pub struct SolveResult {
    pub assignments: Vec<CombinationAssignment>,
    pub remaining: Vec<RemainingItem>,
    pub total_used: u32,
    pub total_remaining: u32,
    pub solve_time_ms: u64,
    pub algorithm: String,
}
```

### 7.3 Tauri IPC 命令

```rust
#[tauri::command]
async fn solve_optimal(request: SolveRequest) -> Result<SolveResult, String>;

#[tauri::command]
async fn save_config(config: AppConfig) -> Result<(), String>;

#[tauri::command]
async fn load_config() -> Result<Option<AppConfig>, String>;
```

---

## 八、开发阶段与里程碑

### 阶段一：项目搭建（1-2 天）

| 任务 | 说明 |
|------|------|
| 初始化 Tauri 2.0 项目 | `npm create tauri-app` |
| 配置 React + TypeScript + Vite | 前端工具链 |
| 配置 Tailwind CSS | 原子化样式 |
| 配置 Zustand | 状态管理 |
| 验证 Tauri IPC 通信 | 写一个 hello world command 测试 |
| 配置 Rust 依赖 | good_lp, serde, serde_json |

**里程碑 M1**：项目可运行，前后端 IPC 通信验证通过。

### 阶段二：输入界面（2-3 天）

| 任务 | 说明 |
|------|------|
| 户型类型编辑器 | 添加/删除/编辑户型，实时更新总数 |
| 组合方案编辑器 | 选择户型、设置数量，可视化预览组合构成 |
| 手动数量输入 | 选择组合、输入数量、校验合法性 |
| 输入校验 | 数量非负、不超过库存等 |

**里程碑 M2**：可完整输入户型和组合，数据通过 Zustand 管理。

### 阶段三：核心算法（2-3 天）

| 任务 | 说明 |
|------|------|
| ILP 求解器实现 | 使用 good_lp 构造并求解 |
| 贪心降级算法 | 回溯+贪心兜底 |
| 求解结果序列化 | Rust → JSON → 前端 |
| Tauri command 集成 | 前端调用、错误处理 |
| 边界情况处理 | 无解、空输入、超大规模 |

**里程碑 M3**：输入数据可计算最优解，结果正确。

### 阶段四：网格可视化（2-3 天）

| 任务 | 说明 |
|------|------|
| Canvas 网格渲染 | 计算行列、绘制方格 |
| 颜色映射 | 组合→颜色，未使用→灰色 |
| 着色动画 | 逐格亮起过渡动画 |
| 悬浮提示 | 鼠标悬停显示信息 |
| 自适应布局 | 响应窗口大小变化 |

**里程碑 M4**：可视化完成，计算结果实时反映在网格上。

### 阶段五：结果面板与交互（1-2 天）

| 任务 | 说明 |
|------|------|
| 结果展示 | 组合数量、剩余明细 |
| 重新计算 | 修改输入后一键重算 |
| 状态栏 | 计算耗时、算法类型 |

**里程碑 M5**：完整交互流程可用，输入→计算→可视化→结果闭环。

### 阶段六：数据持久化与导出（1-2 天）

| 任务 | 说明 |
|------|------|
| 配置保存/加载 | JSON 文件读写 |
| 自动保存 | 修改后自动持久化 |
| 导出为图片 | Canvas → PNG |
| 导出为 JSON | 结果数据导出 |

**里程碑 M6**：数据可持久化，结果可导出。

### 阶段七：测试与优化（2-3 天）

| 任务 | 说明 |
|------|------|
| 算法单元测试 | Rust 端求解器测试 |
| 前端组件测试 | React 组件渲染测试 |
| 边界情况测试 | 0 套、超大数量、无组合等 |
| 性能优化 | 大数量网格渲染优化 |
| UI 打磨 | 间距、字体、颜色微调 |

**里程碑 M7**：测试通过，性能达标。

### 阶段八：打包与发布（1-2 天）

| 任务 | 说明 |
|------|------|
| macOS 构建 | .dmg 安装包 |
| Windows 构建 | .msi/.exe 安装包 |
| 应用图标设计 | 多分辨率图标 |
| README 文档 | 使用说明 |

**里程碑 M8**：可分发的安装包。

### 时间线总览

```
周1:  ████████░░░░  M1 项目搭建 → M2 输入界面 → M3 核心算法
周2:  ████████░░░░  M4 可视化 → M5 结果面板 → M6 持久化
周3:  ██████░░░░░░  M7 测试优化 → M8 打包发布
```

预计总工期：**12-15 个工作日**。

---

## 九、风险评估与对策

| 风险 | 等级 | 影响 | 对策 |
|------|------|------|------|
| good_lp 在某些平台编译困难 | 中 | 无法使用 ILP 求解器 | 预留贪心降级方案；或改用纯 Rust 实现的 `mic_lp` |
| Canvas 大量方格渲染卡顿 | 低 | 1000+ 方格时掉帧 | 使用离屏 Canvas；虚拟化渲染（只渲染可见区域） |
| 用户输入组合无法满足 | 低 | 求解器返回无解 | 明确提示"当前组合无法满足，请调整输入" |
| Tauri 2.0 API 变更 | 低 | 兼容性问题 | 锁定版本号；关注 Tauri 更新日志 |
| macOS/Windows WebView 差异 | 中 | 样式不一致 | 使用 Tailwind 重置样式；测试两个平台 |
| 跨平台编译环境搭建 | 中 | 无法交叉编译 | 使用 GitHub Actions CI 自动构建 |

---

## 十、测试计划

### 10.1 算法测试用例

| 用例 | 输入 | 期望输出 |
|------|------|----------|
| 基本最优解 | 3 种户型(20,30,25)，3 种组合，手动指定 1 个 | 剩余最少 |
| 完美匹配 | 组合恰好用完所有房源 | 剩余 0 |
| 无法用完 | 组合无法整除库存 | 剩余最小化 |
| 单组合 | 只有 1 种组合 | 全部分配给该组合 |
| 零库存 | 某户型数量为 0 | 不影响其他计算 |
| 超大输入 | 1000 套房源 | 计算耗时 < 100ms |
| 无解场景 | 手动输入超出库存 | 返回错误提示 |

### 10.2 前端测试用例

| 用例 | 操作 | 期望 |
|------|------|------|
| 添加户型 | 点击"添加"，输入名称和数量 | 列表更新，总数更新 |
| 删除户型 | 点击删除 | 列表更新，组合中引用同步处理 |
| 定义组合 | 选择户型，设置数量 | 组合预览正确 |
| 计算最优解 | 输入手动数量，点击计算 | 网格着色，结果面板更新 |
| 修改重算 | 修改手动数量 | 网格重新着色 |
| 导出图片 | 点击导出 | 下载 PNG 文件 |
| 持久化 | 关闭后重新打开 | 配置自动恢复 |

### 10.3 性能指标

| 指标 | 目标 |
|------|------|
| 求解耗时（100 套） | < 10ms |
| 求解耗时（1000 套） | < 100ms |
| 网格渲染（100 格） | < 16ms (60fps) |
| 网格渲染（1000 格） | < 50ms |
| 应用启动时间 | < 2s |
| 内存占用 | < 100MB |

---

## 十一、后续扩展方向

1. **多方案对比**：同时展示多种最优解方案，供用户选择
2. **约束条件增强**：支持"某组合至少 N 个""某户型必须用完"等约束
3. **历史记录**：保存历次计算结果，支持回溯对比
4. **批量导入**：从 Excel/CSV 导入户型和组合数据
5. **3D 可视化**：将方格替换为楼层/楼栋的 3D 模型
6. **多语言**：支持中英文切换
7. **云端同步**：多设备配置同步（可选，需后端服务）

---

## 附录：关键依赖版本

```json
// package.json (前端)
{
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "zustand": "^4.5.0",
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "tailwindcss": "^3.4.0"
  }
}
```

```toml
# Cargo.toml (后端)
[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
good_lp = { version = "1", default-features = false, features = ["cbc"] }
```
