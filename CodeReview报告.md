# 房源组合最优解计算器 — Code Review 报告

**审查日期**：2026-08-16
**审查范围**：Rust 求解器（`src-tauri/src/solver/`、`models.rs`、`lib.rs`、`storage.rs`）、前端求解与可视化（`src/utils/jsSolver.ts`、`GridCanvas.tsx`、`useAppStore.ts`）
**审查重点**：贪心算法正确性与最优性、求解公平性
**验证方法**：静态审查 + 单元测试核查 + 可复现实验（针对公平性问题构造反例）

---

## 一、总体结论

| 维度 | 评级 | 说明 |
|------|------|------|
| 正确性 | ★★★★☆ | 约束满足、空组合跳过、手动输入校验均正确，9 个单元测试通过 |
| 最优性 | ★★★☆☆ | ILP 路径精确最优；贪心降级路径为近似解，多起点缓解但**无最优性保证** |
| 公平性 | ★★☆☆☆ | **规划文档承诺的"多最优解公平规则"未实现**，等优解选择完全取决于策略/求解器内部 |
| 健壮性 | ★★★☆☆ | 无求解超时保护；前端静默掩盖 Rust 错误 |
| 可维护性 | ★★★☆☆ | JS/Rust 双实现逻辑漂移；代码结构清晰，注释充分 |

**最重要发现**：
1. **公平性缺失（高）**——存在多个总套数相同的等优解时，结果无公平性保证（实验证实）。
2. **贪心无最优性保证（中）**——多起点贪心可能错过最优解，且与 ILP 结果不一致。
3. **Rust 求解无超时保护（中）**——组合数接近 60 时 minilp 分支定界可能耗时过长。
4. **错误静默掩盖（中）**——桌面版求解失败会被前端静默降级，用户无感知。

---

## 二、贪心算法审查

### 2.1 实现机制（greedy.rs / jsSolver.ts）

- 多起点贪心：4 种排序策略（Rust）/ 3 种（JS）——按总套数降序、总套数升序、原始顺序、平均每户型套数升序（Rust 独有）
- 每种策略顺序分配：`while (剩余 ≥ 组合需求)` 逐份扣减
- 取总使用套数最大的方案

### 2.2 发现与风险

**[G1] 贪心是近似解，无最优性保证（中）**
- 贪心本质是"顺序敏感"的局部最优，多起点显著缓解但无法保证。
- 实验 1（`usage=[[2,0],[1,1]]`, 库存 `(4,2)`，最优 6 套）：
  - "大优先"策略 → `x=[2,0]`，仅用 **4 套**（损失 2 套）
  - "小优先"策略 → `x=[1,2]`，用 6 套（恰为最优）
- 结论：**单个策略可能损失可观的套数**；当前 4 策略并行恰好覆盖了常见反例，但构造同时失败的反例在理论上是可能的（贪心无最坏情况保证）。

**[G2] 贪心与 ILP 结果不一致（中）**
- 桌面版（Rust ILP）与浏览器预览（JS 贪心）可能给出不同分配；同一用户在两处看到不同结果，易造成困惑。
- 风险点：预览模式在 `GridCanvas` 角标显示"JS 预览"，但结果面板未提示"预览结果非精确解"。

**[G3] 平局处理依赖策略顺序（中，兼公平性问题）**
- `if used > best.used`（严格大于）→ 多个策略产出相同总套数时，**保留第一个策略的结果**（大优先优先）。
- 结合 [F2]，平局时的最终方案由策略枚举顺序决定，无任何业务倾向。

**[G4] 性能：逐份扣减可优化（低）**
- `while` 每分配一份遍历全部户型；当库存大（如 10 万套）且组合小（1 套）时循环 10 万次 × 户型数。
- 可优化为比率批量分配：`x_k = min_j(⌊rem[j] / usage[k][j]⌋)` 一步到位。
- 实测 99999 套单组合约 5ms，当前规模无碍；但组合多、库存大时建议优化。

**[G5] JS/Rust 贪心逻辑漂移（低）**
- JS 版缺 Rust 的策略 4（轻组合优先），两处实现会随演进渐行渐远；建议以 Rust 为准，JS 版注明"简化预览实现"。

---

## 三、公平性审查（重点）

### 3.1 规划承诺 vs 实际实现

规划文档明确承诺：
> "当存在多个使用相同总数量的方案时：① 剩余户型种类最少 ② 组合数量方差最小 ③ 组合编号最小"

**实际代码未实现任何公平性规则**——ILP 目标函数只有"最大化总套数"，贪心只取"总套数最大"。多最优解时的选择完全交给求解器内部行为。

### 3.2 发现的公平性问题

**[F1] 等优解选择任意，无公平性保证（高）**
- 实验 2（户型 A×10；组合1=(1)、组合2=(2)）：存在 **6 个总套数相同的等优解**（组合1=10&组合2=0 / 组合1=0&组合2=5 / 组合1=6&组合2=2…）。
- 实测 jsSolve 返回 `组合1=0, 组合2=5`——把所有货压在一个组合上。
- **业务影响**：同样"用完 10 套"的前提下，"均衡分配"（组合1≈6, 组合2≈2）通常比"单一组合独占"（组合1=10）更符合销售诉求；当前结果取决于策略顺序/求解器内部，用户无法预期。
- 这是本次审查**最需要优先解决**的问题。

**[F2] 组合顺序偏见（高）**
- 实验 3：两个完全相同的组合（各含 1 套同户型），库存 6 → 结果 `先定义=6, 后定义=0`。
- 贪心排序与 ILP 变量顺序均以组合索引为 tie-break，**先定义的组合在平局时永远优先**，后添加的组合可能长期分配不到。

**[F3] 剩余分布不均衡（中）**
- 最优解可能把剩余集中到少数户型（甚至单一户型），与"尽量用完"目标一致但可能不符合业务期望（如希望各户型剩余均衡、可单独去化）。规划中的"剩余户型种类最少"未落实。

**[F4] 手动输入改变后自由组合的分配倾向（低）**
- 手动指定某组合后，剩余库存给自由组合；自由组合仍受 F1/F2 影响，且结果对"谁被手动指定"敏感。

### 3.3 公平性改进建议（按可实现性排序）

| 方案 | 描述 | 复杂度 | 效果 |
|------|------|--------|------|
| **A. 均衡化后处理（推荐先做）** | ILP/贪心求解后，在保持总套数不变的前提下做"转移"：尝试 `x_a−1, x_b+1`（不违反约束、总套数不变），选择使**组合数量方差最小**的方案，迭代至稳定 | 低（几十行） | 直接修复 F1，不依赖求解器 |
| **B. 二级目标（中后期）** | 第一目标最大化总套数；在其最优值约束下，第二目标最小化剩余户型种类数、最小化组合数量方差（两次求解或加权目标） | 中 | 完整实现规划承诺 |
| **C. 贪心平局公平化** | 平局时比较"剩余户型种类数、组合数量方差"而非保留首个策略；tie-break 去掉索引 | 低 | 修复 F2 在贪心路径的表现 |
| **D. 组合顺序轮转（可选）** | 提供"公平轮转"模式：多次以不同组合顺序求解取均衡解，或让用户拖动排序 | 低 | 缓解 F2 的"先定义优先" |

---

## 四、其他问题

| 编号 | 位置 | 问题 | 等级 |
|------|------|------|------|
| O1 | `useAppStore.safeSolve` | invoke 失败一律静默降级 JS：**桌面版真实求解错误（如库存不足的 Err）被吞掉**，用户看到的是 JS 结果而非错误提示。应区分 `NOT_IN_TAURI`（预览环境）与真实错误 | 中 |
| O2 | `ilp.rs` | 组合数 60 以下才走 ILP，但 minilp 分支定界无时间限制，接近上限时可能耗时数秒～数十秒；前端 5s 超时降级 JS 但 Rust 线程仍占用 CPU | 中 |
| O3 | `solver/mod.rs` | `total += item.count`（u32）无溢出保护；极端输入（count≈4e9）在 debug 构建 panic。建议 `saturating_add` | 低 |
| O4 | `ilp.rs` | 目标/约束系数转 f64，整数范围内安全；`.round()` 兜底合理 | 通过 |
| O5 | `GridCanvas` | 着色按组合列表顺序"先到先得"占格子——纯视觉呈现，非逻辑问题，但颜色与组合的对应在组合增删后可能跳变 | 低 |
| O6 | 测试覆盖 | 有正确性测试（含空组合、非法输入），但**缺公平性/最优性回归测试**（如实验 2/3 场景） | 中 |

---

## 五、改进优先级

| 优先级 | 事项 | 对应问题 |
|--------|------|----------|
| P0 | 均衡化后处理：等优解按组合数量方差最小选择 | F1 |
| P0 | 修复错误静默掩盖：区分预览降级与真实错误 | O1 |
| P1 | 组合顺序 tie-break 公平化 | F2 |
| P1 | Rust 求解超时保护（超时降级贪心） | O2 |
| P1 | 补公平性回归测试（实验 2/3 场景固化为单测） | O6 |
| P2 | 贪心批量分配性能优化 | G4 |
| P2 | JS/Rust 贪心对齐（补策略 4）或明确标注差异 | G5 |
| P2 | u32 溢出防御 | O3 |

---

## 六、结论

项目主体工程完成度高：约束建模正确、防御性编码（saturating、空组合跳过、超时降级）到位，单元测试覆盖了关键正确性场景。

**但"最优解"的定义目前只包含"总套数最大"这一维度，规划中承诺的公平性规则（剩余种类最少、方差最小）缺失**。在真实销售场景中，同一最优套数下的不同分配方案（独占 vs 均衡）业务含义差异显著，当前行为不可预期，这是本轮审查的最核心问题，建议按 P0 优先实施"均衡化后处理"方案（实现简单、不依赖求解器能力）。

其次，Rust 求解器缺少时间上限保护、前端错误被静默掩盖，属中等风险，建议在 P1 一并处理。

---

*附：本报告涉及的实验脚本为临时文件，已清理；复现方式见正文各节输入数据。*
