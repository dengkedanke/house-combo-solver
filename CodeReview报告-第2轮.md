# 房源组合最优解计算器 — Code Review 报告（第二轮）

**审查日期**：2026-08-16（P0/P1/P2 修复后复审）
**审查范围**：Rust 求解器（`solver/ilp.rs`、`solver/greedy.rs`、`solver/mod.rs`）、前端求解（`utils/jsSolver.ts`、`store/useAppStore.ts`）
**审查方法**：静态审查 + 单元测试核查 + 可复现实验
**与首轮关系**：验证首轮 P0/P1/P2 修复的正确性，并排查修复引入的新问题

---

## 一、修复验证结论

首轮报告提出的 P0/P1/P2 共 8 项问题，复审确认**全部正确落地**：

| 编号 | 问题 | 状态 | 验证 |
|------|------|------|------|
| P0-F1 | 等优解公平性 | ✅ 已修复 | `balance_solution` 转移迭代，回归测试 `balance_prefers_even_distribution`（x1=2,x2=4）、`balance_removes_order_bias`（3/3） |
| P0-O1 | 错误静默掩盖 | ✅ 已修复 | `safeSolve` 三分支（预览/超时/真实错误） |
| P1-F2 | 平局 tie-break | ✅ 已修复 | 贪心选优加入方差比较 |
| P1-O2 | ILP 超时 | ✅ 已修复 | 线程 + `recv_timeout(2000ms)` |
| P1-O6 | 公平性回归测试 | ✅ 已补 | `transfer_unit_math` + 2 个公平性用例 |
| P2-G4 | 批量分配 | ✅ 已修复 | 999999 套 0ms |
| P2-G5 | 策略4 对齐 | ✅ 已修复 | JS 补轻组合优先，实验证实提升最优性 |
| P2-O3 | 溢出防御 | ✅ 已修复 | `saturating_add` |

---

## 二、新发现问题（本轮）

### N1（P0 · 已修复）JS 策略 1/2 排序索引错位，产生 NaN 比较器

**位置**：`utils/jsSolver.ts` 贪心排序构造

**问题**：`totals` 数组按 `freeUsage` 下标（0..自由组合数-1）排列，但排序比较器直接使用**全局组合索引**作下标：

```typescript
const totals = freeUsage.map(...);                    // 长度 = freeIdx.length
orders.push([...freeIdx].sort((a, b) => totals[b] - totals[a] || a - b));
//                                   ^^^^^^^^ a、b 是全局索引
```

当存在手动输入时，`freeIdx` 非连续（如 `[0, 2]`），`totals[2]` 越界为 `undefined`，比较器返回 `NaN` → 排序进入**未定义行为**，策略 1/2 完全失效。

**影响**：预览模式 + 手动输入场景下，两个排序策略失效，结果退化且不可预测（当前恰好被策略 3/4 兜底，但属于隐性炸弹）。实测复现：`1 - undefined = NaN`。

**修复**：引入 `pos` Map 统一映射全局索引 → freeUsage 下标：

```typescript
const pos = new Map(freeIdx.map((k, i) => [k, i]));
orders.push([...freeIdx].sort((a, b) => totals[pos.get(b)!] - totals[pos.get(a)!] || a - b));
```

策略 4 与 `solveGreedyOneOrder` 调用处同步改用 `pos`，顺带消除了 `freeIdx.indexOf` 在比较器内的 O(n²) 复杂度（见 N4）。

---

### N2（P1）ILP 每次求解 spawn 线程，存在堆积风险

**位置**：`solver/ilp.rs`

**问题**：`solve_ilp` 每次调用都 `std::thread::spawn`。自动计算（300ms 防抖）在用户连续输入时会高频调用；若多次超时，minilp 无法强制中断，**多个后台线程会同时占用 CPU**。

**影响**：正常规模（<2s）无碍；极端场景（组合数接近 60 且难解）可能 CPU 短暂飙高。

**建议**：增加"单飞（single-flight）"标志，前一次未结束时跳过新求解；或复用线程。

---

### N3（P2）均衡化局部最优，非全局方差最小

**位置**：`balance_solution`（Rust）/ `balanceSolution`（JS）

**问题**：单步 lcm 转移是贪心式局部搜索，可能卡在局部最优（方差非全局最小）。例如三个组合互相牵连时，两两转移可能无法到达全局均衡。

**影响**：实际销售场景（组合数少）已验证能达到满意均衡；理论边界存在但无碍。

**建议**：如需严格全局最优，可改为带随机重启的局部搜索，或直接求解二次目标（需换支持二次的 solver）。

---

### N4（P2）JS 排序比较器 O(n²) 复杂度

**位置**：`jsSolver.ts` 策略 4 的 `freeIdx.indexOf` 在 sort 比较器内

**问题**：每次比较都 `indexOf`（O(n)），排序共 O(n log n) 次比较 → O(n² log n)。组合数 ≤60 时无感，但代码不够整洁。

**状态**：已随 N1 修复一并优化为 `pos` Map 常量时间查找。

---

### N5（信息）预览与桌面结果仍可能不一致

**说明**：浏览器预览走 JS 贪心（4 策略 + 均衡化），桌面走 Rust ILP（精确 + 均衡化）。均衡化只保证"同等总套数下的公平"，但贪心起点（总套数）可能低于 ILP 最优值，故两环境结果仍可能不同。这是"预览 vs 精确"的**设计取舍**，非缺陷；建议在预览结果面板加"预览结果仅供参考"提示。

---

## 三、质量指标

| 维度 | 首轮 | 本轮 | 变化 |
|------|------|------|------|
| 正确性 | ★★★★☆ | ★★★★☆ | 修复 N1 后更稳 |
| 最优性 | ★★★☆☆ | ★★★★☆ | 策略4 补齐 + 均衡化 |
| 公平性 | ★★☆☆☆ | ★★★★★ | 均衡化 + 平局方差选择 |
| 健壮性 | ★★★☆☆ | ★★★★☆ | ILP 超时保护 |
| 可维护性 | ★★★☆☆ | ★★★★☆ | 双端对齐、注释完善 |

---

## 四、结论

首轮提出的 P0/P1/P2 问题已全部正确修复，公平性从 ★★ 提升至 ★★★★★，本轮复审额外发现并**已修复**一个真实的正确性隐患（N1 排序索引错位），其余为新发现的低优先级改进项（N2/N3/N4 已部分处理，N5 为设计取舍）。

建议后续：① 落实 N2 单飞保护；② 预览面板标注"仅供参考"；③ 如需严格全局公平，考虑二次目标求解。当前代码已具备发布条件。

---

*附：N1 已在本轮修复并通过回归测试（手动输入 + 多自由组合场景，库存全用完）；实验脚本为临时文件，已清理。*
