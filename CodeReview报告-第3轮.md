# 房源组合最优解计算器 — Code Review 报告（第三轮）

**审查日期**：2026-08-18
**审查范围**：新增"遍历备选方案"（ILP 增量割枚举）与"≥1"数量下界约束两大功能，含 Rust 求解器（`ilp.rs`/`mod.rs`）与前端（`jsSolver.ts`/`store`/`InputPanel`/`ResultPanel`）
**审查方法**：静态审查 + 单元测试核查 + 边界推演
**与既往关系**：首轮（贪心与公平性）、次轮（P0/P1/P2 复审）已闭环，本轮聚焦新功能引入的风险

---

## 一、新增功能审查结论

### 1.1 遍历备选方案（enumerate_solutions + no-good cut）

**实现正确性：通过**（有测试 + 推演双重确认）

| 审查点 | 结论 |
|--------|------|
| no-good cut 数学（Σₚx − Σ_zx ≤ Σₚx* − 1） | ✅ 正确：精确屏蔽已求得的解 x*，且不误伤真正不同的等优解（P 上更挤的解被排除属预期多样化行为） |
| 均衡化与枚举冲突 | ✅ 已规避：枚举保存 ILP 原始解（不均衡化），seen 去重兜底——解决了"均衡转移把不同解映射为同一展示解导致重复"的坑 |
| 全 0 空方案 | ✅ 已过滤（无手动输入时全 0 视为遍历结束，不生成空方案标签） |
| 与"≥1"下界共存 | ✅ 变量下界每轮保留，cut 只屏蔽解向量，两者正交 |
| 上限保护 | ✅ max_solutions 默认 50、上限 200；前端 30s 超时 |

### 1.2 "≥1"数量下界约束（min_one_combination_ids）

**实现正确性：通过**（校验完备 + 全链路贯通）

| 审查点 | 结论 |
|--------|------|
| 校验覆盖 | ✅ 不存在组合 / 空组合 / 单组合库存不足 / **多个下界合计超库存** 均返回明确错误 |
| ILP / 贪心 / 均衡化 / 枚举 | ✅ 四处均支持下界：变量下界、贪心预分配、转移下界保护、每轮透传 |
| 冲突处理 | ✅ 手动指定（>0）天然满足且不冲突；下界合计校验保证 ILP 恒有可行解、贪心预分配不下溢 |
| 前端一致性 | ✅ 复选框（空组合禁用）→ store → 请求 → JS/Rust 求解 → 结果区"≥1"标签全链路一致 |

---

## 二、新发现问题

### E1（P2）枚举循环的线程堆积与时长失控风险

**位置**：`enumerate_solutions` 循环 × `solve_ilp_with_cuts`（每轮 spawn 线程 + 2s 超时）

**问题**：每轮求解都新建线程；若多轮均触发超时，最坏 50 轮 ≈ **100s 且 50 个后台 minilp 线程同时占用 CPU**（线程无法强制中断）。前端 30s 超时会先降级 JS 枚举，但 Rust 线程仍在后台跑。

**建议**：枚举路径缩短单轮超时（如 1s）；或增加总时间预算（累计超时即终止）；或复用单飞线程。

### E2（P2）"已穷尽"与"因超时截断"不可区分

**位置**：`enumerate_solutions` 的 `None => break`

**问题**：单轮超时（minilp 慢）与真正无解都走 `break`，用户无法判断遍历是"全部枚举完"还是"超时提前结束"，可能误以为结果完整。

**建议**：区分终止原因（超时 vs 无解），在返回结果的 algorithm 字段或前端标注"因超时截断，仅部分方案"。

### M1（P3）手动指定数量的组合仍可勾选"≥1"

**位置**：`InputPanel.tsx` 复选框（仅空组合禁用）

**问题**：手动指定数量 > 0 的组合勾选"≥1"无实际效果（后端 `if manual[k] { continue }` 忽略），但 UI 无反馈，用户可能困惑。

**建议**：手动指定数量 > 0 时同样禁用该复选框（与空组合一致的交互语义）。

### M2（P3）"≥1"勾选状态不持久化

**位置**：`saveConfig` 仅存 houseTypes/combinations

**问题**：重启后 minOneIds 勾选丢失（与 manualInputs 行为一致，属既有取舍，但新约束建议纳入持久化以降低用户负担）。

**建议**：AppConfig 增加 `minOneCombinationIds` 字段随配置保存/加载。

### E3（P3）浏览器预览与桌面枚举结果不同

**位置**：`jsEnumerate`（贪心 + 子集变体） vs Rust ILP 枚举

**问题**：预览环境生成的是近似方案集（多策略贪心 + 非下界组合子集变体），与桌面端精确枚举的方案数量/内容不同；algorithm 字段虽有标注，界面无提示。

**建议**：预览结果面板提示"预览模式为近似枚举，桌面版为精确遍历"。

---

## 三、回归确认（既往修复项）

| 轮次 | 修复项 | 状态 |
|------|--------|------|
| 首轮 P0 | 等优解公平性均衡化 / 错误静默掩盖 | ✅ 保持有效 |
| 首轮 P1 | 平局 tie-break 公平化 / ILP 超时保护 | ✅ 保持有效 |
| 首轮 P2 | 贪心批量分配 / 策略4 对齐 / 溢出防御 | ✅ 保持有效 |
| 次轮 N1 | JS 排序索引错位 | ✅ 保持有效（新功能未引入同类问题） |

单元测试 23/23 通过（含枚举去重/穷尽/上限、下界强制纳入/库存不足/合计超限/枚举保留等用例），前端构建通过。

---

## 四、总体结论

| 维度 | 评级 | 说明 |
|------|------|------|
| 正确性 | ★★★★☆ | 两新功能实现正确、测试覆盖充分；E2 的终止语义是唯一正确性相关的模糊点 |
| 公平性 | ★★★★★ | 均衡化 + 平局方差 + 下界保护，无新退化 |
| 健壮性 | ★★★☆☆ | E1 线程堆积/时长失控是本轮最需关注的健壮性缺口 |
| 可维护性 | ★★★★☆ | prepare/assemble 提取后结构清晰，注释完整；JS/Rust 双实现保持同步 |

**建议处理优先级**：E1、E2 为 P2（枚举场景的健壮性与语义完整）；M1、M2、E3 为 P3（体验与持久化打磨）。核心算法与数据流无正确性缺陷，具备发布条件。

---

*附：本报告基于对 `ilp.rs`（Cut/变量下界）、`mod.rs`（prepare 校验/enumerate 循环/balance 下界保护）、`jsSolver.ts`（预分配/子集变体）的静态审查与边界推演。*
